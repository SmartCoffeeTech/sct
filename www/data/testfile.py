cp_list
each coffee in the cp_list
jsons for each coffee in the cp_list

cp_list = grinder_pct_list
grinder0 connects to coffee0 ...n
static grinder_pct_list -> blend_pct_list

only do this in custom phase